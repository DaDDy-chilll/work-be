name: Docker Build and Push
on:
  push:
    branches:
      - development
      - staging
      - production
      - devops
    tags:
    - '*'
    
jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Setup AWS ECR
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-south-1 #${{secrets.AWS_REGION}}

      - name: Login to Amazon ECR
        id: login-pf-aws-ecr
        uses: aws-actions/amazon-ecr-login@v1

      - name: Build and push the tagged docker image to Amazon ECR
        env:
          ECR: ${{ steps.login-pf-aws-ecr.outputs.registry }}
          REPO: hospitalmyanmar
          BRANCH_NAME: ${{ github.ref_name }} # Branch name
          
        run: |
          IMAGE_TAG="parami-api-$BRANCH_NAME" 
          docker build -t $ECR/$REPO:$IMAGE_TAG .
          docker push $ECR/$REPO:$IMAGE_TAG

  deploy:
    name: deploy
    needs: build
    runs-on: ubuntu-latest
      - name: Deploy to Development
        if:  github.ref == 'refs/heads/development' 
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.Hospital_DEV_EC2_IP }}
          username: ${{ secrets.Hospital_DEV_EC2_USER }}
          key: ${{ secrets.Hospital_DEV_SSH_PRIVATE_KEY }}
          command_timeout: 30m
          script: |
            cd /home/ubuntu/devops/
            bash parami-dev-deploy.sh

      - name: Deploy to Production
        if:  github.ref == 'refs/heads/production' 
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.Hospital_PROD_EC2_IP }}
          username: ${{ secrets.Hospital_PROD_EC2_USER }}
          key: ${{ secrets.Hospital_PROD_SSH_PRIVATE_KEY }}
          command_timeout: 30m
          script: |
            cd /home/ubuntu/devops/
            bash parami-production-deploy.sh